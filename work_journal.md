# work_journal — журнал действий (append-only)

Формат: `| время | что просили | что сделал | скилл |` — правила в `AGENTS.md` §2.

Ниже — обезличенный фрагмент реального журнала: строки переписаны без проектной
специфики, но сохраняют жанр и уровень детализации. В живой системе журнал —
самый важный файл директории; за первые двое суток работы он набрал ~40 строк.

| Время | Что просили | Что сделал | Скилл |
|---|---|---|---|
| 2026-08-19 23:07 | изучить Гант и составить план работ для себя и команды с артефактами | созданы team/{README,STATUS,PLAN,TRACK_*,RISKS,DECISIONS}.md: сверка Ганта с фактом репозитория, план по неделям, развилки и риски с владельцами | да: разбор xlsx парсером stdlib без openpyxl |
| 2026-08-20 00:49 | записать AGENTS.md для контекстной директории: домены, журнал, deprecated | создан AGENTS.md; заведены skills/ и .other/; журнал инициализирован этой таблицей | нет |
| 2026-08-20 01:05 | статус по направлениям + таск-трекер в CSV | статусы собраны; создан tracker/{README.md,tasks.csv,goals.csv}: задачи, цели, проекты, вехи; AGENTS.md дополнен §5 и чек-листом | нет |
| 2026-08-20 02:10 | мьютексы на файлы + worktree и локальный git для параллельных агентов | создан tools/ctx_lock.py (mkdir-мьютексы, TTL, перехват протухших — протестирован); контекст стал локальным git-репо (merge=union для журнала); AGENTS.md §8 + чек-лист | да — протокол лока+worktree для параллельных агентов |
| 2026-08-20 04:20 | оценить реорганизацию: tracker и team переехали в мастерскую | принята граница «домены=предмет, мастерская=координация»; создан tools/check_refs.py — нашёл 24 битые ссылки после переезда, все исправлены | нет |
| 2026-08-20 04:50 | витрины симлинками (диздоки draft/published, частые скиллы) + устав rules/ | диздоки переехали на кухню mastery/design_docs, в корне витрина с README (публикация = симлинк владельца); витрина skills/ с 4 симлинками; создан устав rules/rule-1..10 + README | нет |
| 2026-08-20 12:20 | где зафиксированы остальные волны работ? | выяснилось: нигде — точная разбивка жила только в чате, а таблица в доменном обзоре устарела. Создан доменный WAVES.md; две копии цифр заменены одной со ссылкой. Попутно: верификатор волны нашёл 2 дефекта в собственных инструментах | да — «негативный контроль верификатора: проверка обязана падать на намеренно сломанном входе» |
| 2026-08-20 23:40 | UI для трекера; постоянная синхронизация БД и файла, БД first + dump | схему «БД first» НЕ реализовал: она нарушает rule-3 («две копии расходятся всегда»), а rule-10 разрешает менять устав только владельцу. Вместо обхода устава устранена настоящая причина недовольства — JSON в ячейках CSV: связи → links.csv (ребро хранится один раз), комментарии → comments/<КЛЮЧ>.md; дифф стал +1/+4 строки вместо всего файла | да — «JSON в ячейке CSV: почему это плохо и как разносить на рёбра+прозу без потери данных» |
| 2026-08-21 00:05 | UI трекера на файлах; главное — чтобы правки фиксировались | сделан локальный веб-UI (stdlib, только localhost) + ядро записи. Гарантии: лок ctx_lock, атомарная запись os.replace+fsync, git-коммит на каждую правку. Найден и исправлен свой баг: writer писал LF в CRLF-файл — git показывал все 38 строк изменёнными при правке одного поля | да — «атомарная запись в текстовый источник истины: лок, os.replace, fsync каталога, сохранение стиля перевода строк» |
| 2026-08-21 13:45 | зафризить систему ведения проекта в публичный репозиторий | собран этот репозиторий: каркас контекстной директории без проектной специфики, трекер подключён сабмодулем | нет |
| 2026-08-21 16:09 | полноценная сборка Herdr+dsh в octo, Telegram-бот, удалённый клиент | заведён первый живой домен agent-infra/ (по EXTENDING.md, который написан этим же коммитом): инструкция стека, up.sh (workspace octo, панель dsh web :3081, панель бота, смоук), bin/dsh-herdr, tg/dsh_tg.py (конфиг из env, fail-closed, унаследованный баг state=done исправлен на idle — такого статуса у report-agent нет); скиллы MST-30..32, GOAL-2/PRJ-2/INFRA-1..6 в трекере, черновик диздока herdr_dsh_stack на кухне. Плагины dsh проверены: telegram/remote в комплекте нет — бот кастомный, удалёнка через Tailscale+SSH (INFRA-5, беклог) | скилл: да — report-agent/headless-обёртка/tg-remote оформлены сразу (MST-30..32) |
| 2026-08-21 16:35 | а не должны ли dsh и herdr быть сабмодулями? | решение Дениса после разбора вариантов: reference-сабмодули agent-infra/vendor/{herdr,deepseek-harness}, запинены на проверенные ревизии (v0.8.2 → 9eb5214, dsh-v0.1.0-rc.7 → 99f6f02), shallow-клоны; это референс для чтения, не дерево сборки — установка остаётся пакетной; check_refs не сканирует vendor/, README предупреждает о весе --recurse-submodules | скилл: нет |
| 2026-08-21 16:40 | в главный README — как поднять стек с harness внутри Herdr | секция «Быстрый старт: harness внутри Herdr»: предпосылки (install.sh herdr / npx dsh — команды сверены с vendored-доками, не по памяти), herdr → clone → up.sh, что получается и куда идти дальше; деталям — ссылка на инструкцию (rule-3) | скилл: нет |
| 2026-08-21 16:51 | Денис всерьёз замокал дизайн мобильного приложения (Hearth.html в корне) | артефакт разобран из бандла (x-dc: 9 экранов + варианты 1b–1g) и перенесён в домен: agent-infra/hearth/{Hearth.html,README.md}; заведён INFRA-7 (ревью, связи с INFRA-1/5); ревью агента с голосами по вариантам и тремя развилками (hearthd, источник approvals, транспорт) — comments/INFRA-7.md; .idea/ в .gitignore | скилл: нет |
| 2026-08-21 17:05 | делегирован INFRA-8: сквозная проверка петли «кнопка → агент → нотификация» | исполнено по MST-33: блокеров нет, статус разработка→ревью под локом, создан agent-infra/generated/delegation_probe.md (16 задач в tasks.csv, дата прогона), комментарий в comments/INFRA-8.md, строки в usage_log (MST-33/12/18) | скилл: нет |
| 2026-08-21 17:08 | мечта: кнопка «отдать агенту» в трекере, скилл-исполнитель, нотификации, трекер и реальное iOS в Hearth | петля делегирования v0 СОБРАНА И ПРОВЕРЕНА СКВОЗНЫМ ПРОГОНОМ: скилл task_executor (MST-33, by_team, evals) + bin/delegate (контекст тикета из CSV → dsh headless через dsh-herdr → маркер РЕЗУЛЬТАТ → TG-нотификация curl-ом); INFRA-8 исполнен агентом-исполнителем по протоколу (артефакт, коммент, ревью, свой коммит 4e5880d, usage_log MST-33/12/18); нотификация упрётся в «chat not found», пока владелец не нажмёт /start. Тикетами: кнопка в saturday-tracker (INFRA-9), вкладка трекера и нотификации в Hearth (INFRA-10), PWA-сборка (INFRA-11) | скилл: нет — исполнитель уже оформлен как MST-33 |
| 2026-08-21 17:15 | обновить цели (выстраивание Harness — отдельная цель) и подбить статусы проекта | GOAL-3 «Harness выстроен как рабочая среда» заведена (признание объёма работ отдельной целью), PRJ-2 переподчинён ей; GOAL-2 закрыта решённой — agent-infra прошёл полный цикл механики; INFRA-8 закрыт с резолюцией; статусы INFRA-1/4 и PRJ-2 актуализированы; создан agent-infra/STATUS.md — снимок «готово/в работе/беклог» с фактами по rule-7 | скилл: нет |
| 2026-08-21 17:19 | в целях еле-еле деление на эпики — навести нормальную иерархию и lineage | иерархия перестроена: GOAL-3 → PRJ-2 → эпики-треки INFRA-12 (удалёнка) / INFRA-13 (делегирование) / INFRA-14 (Hearth) + INFRA-1 сужен до «сборка стека» и закрыт с уроком «эпик всего не даёт lineage»; 8 рёбер пере-родительствованы, веха MS-2 «тикет отдан с телефона» поперёк треков B+D; lineage-диаграмма сгенерирована штатным tracker_mermaid.graph() и проверена рендером (RENDER_OK + негативный контроль); STATUS.md переснят | скилл: нет — применён MST-26 |
| 2026-08-21 17:25 | иерархию — по всем задачам, не только INFRA | полный аудит lineage: найдены сироты DEMO-3 (без родителя) и DEMO-5 (без единой связи — не попадала в граф), веха MS-1 ни с чем не связана; добавлены 3 ребра (DEMO-3/5 → PRJ-1, DEMO-1 блокирует MS-1) — сирот 0 на 26 узлах; создан mastery/tracker/OVERVIEW.md: обе ветки (живая + демо-образец), 5 инвариантов lineage, счёт по статусам; полный граф проверен рендером | скилл: нет — MST-26 повторно |
