# STATUS — agent-infra

- **Дата снимка:** 2026-08-21 18:10
- **Lineage:** GOAL-3 → PRJ-2 → эпики-треки → задачи; веха MS-2 поперёк треков

## Иерархия

```
GOAL-3 «Harness выстроен как рабочая среда»            [разработка]
└── PRJ-2 «Агентная инфраструктура octo»               [разработка]
    ├── INFRA-1  эпик A «Сборка стека»                 [закрыт: решен]
    │   ├── INFRA-2  up.sh + смоук                     [закрыт: решен]
    │   └── INFRA-3  dsh-herdr                         [закрыт: решен]
    ├── INFRA-12 эпик B «Удалёнка: телефон как пульт»  [разработка]
    │   ├── INFRA-4  Telegram-бот                      [тестирование: ждёт /start]
    │   └── INFRA-5  Tailscale + reattach              [в беклоге]
    ├── INFRA-13 эпик D «Делегирование»                [разработка]
    │   ├── INFRA-8  сквозная проверка петли           [закрыт: решен]
    │   └── INFRA-9  кнопка «отдать агенту» в UI       [в беклоге]
    ├── INFRA-14 эпик E «Hearth: мобильный клиент»     [ревью]
    │   ├── INFRA-7  дизайн (макет Дениса)             [ревью]
    │   ├── INFRA-10 вкладка трекера + нотификации     [в беклоге]
    │   └── INFRA-11 PWA-сборка                        [в беклоге, блокируется INFRA-5]
    └── INFRA-6  диздок «оркестратор стада» (трек C)   [анализ]

MS-2 «Тикет отдан с телефона и вернулся нотификацией»  [в беклоге]
     реализует PRJ-2 · блокируется INFRA-4 и INFRA-9
```

Урок наведения иерархии — в резолюции INFRA-1: «эпик всего» не даёт
lineage, треки должны быть эпиками с первого дня.

## Диаграмма (сгенерирована из данных, проверена рендером)

Генерация — штатным `tracker_mermaid.graph()` из сабмодуля (диаграмма не
рисуется руками — MST-26); рендер проверен headless Chrome + вшитый
mermaid, негативный контроль: сломанный вход даёт RENDER_ERR.

```bash
SATURDAY_TRACKER_DATA=$PWD/mastery/tracker python3 -c "
import sys; sys.path.insert(0, 'mastery/tools/saturday-tracker')
from saturday_tracker import tracker_mermaid as tm
print(tm.graph(['GOAL-3','PRJ-2','MS-2'] + [f'INFRA-{i}' for i in range(1,15)]))"
```

## Ближайшие ходы

1. **`/start` у `@dgreat_harness_bot`** (владелец, один тап) — открывает
   INFRA-4 и нотификации делегирования; после него достижима веха MS-2.
2. INFRA-9 — кнопка в saturday-tracker (обвязка готова).
3. INFRA-5 — Tailscale (один вечер; открывает INFRA-11).
4. Выбор вариантов Hearth 1b–1g (владелец) — двигает эпик E.

## Ключевые факты (rule-7: цифры живут здесь)

- Стек: herdr 0.8.2 · dsh 0.1.0-rc.7 · Node 25.8.1 · порт web `octo` — 3081.
- У `pane report-agent` 4 статуса (`idle|working|blocked|unknown`), `done` нет.
- Модель headless-прогона задаётся только `--patch`-оверлеем.
- Секреты бота — `~/.dsh-tg/env` (вне git); Telegram из python — через curl
  (trust store framework-питона пуст).
- Сквозной прогон делегирования: 1 тикет, 1 прогон, 0 ручных правок после.
- Диаграмма lineage: RENDER_OK, SVG 44 КБ; негативный контроль — RENDER_ERR.
