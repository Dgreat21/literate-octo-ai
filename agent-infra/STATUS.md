# STATUS — agent-infra

- **Дата снимка:** 2026-08-21 17:45
- **Цель:** GOAL-3 «Harness выстроен как рабочая среда» (разработка)
- **Проект:** PRJ-2 «Агентная инфраструктура octo» (разработка)

## Готово (закрыто, решено)

| Что | Тикет | Факт |
|---|---|---|
| Единый вход стека: воркспейс `octo`, dsh web :3081, смоук | INFRA-2 | up.sh идемпотентен, повторный запуск не плодит панели |
| Headless-обёртка с репортом статуса в сайдбар | INFRA-3 | 3+ прогонов, `-m provider/model` через `--patch` |
| Петля делегирования v0: тикет → исполнитель → маркер → нотификация | INFRA-8 | сквозной прогон: агент сам исполнил тикет по MST-33 |
| Скиллы стека | MST-30..33 | в реестре, применения логируются (исполнитель логирует сам себя) |
| Reference-сабмодули апстримов | — | herdr @ v0.8.2, dsh @ rc.7 в `vendor/` |

## В работе / ждёт

| Что | Тикет | Статус | Блокер |
|---|---|---|---|
| Telegram-бот | INFRA-4 | тестирование: запущен, polling жив | **ждёт `/start` владельца** — без него боты не пишут первыми, нотификаций нет |
| Hearth: дизайн клиента | INFRA-7 | ревью | выбор вариантов 1b–1g за владельцем |
| Диздок «dsh как оркестратор стада» | INFRA-6 | анализ | — |

## Беклог (очерёдность предложена)

1. INFRA-9 — кнопка «отдать агенту» в saturday-tracker (обвязка готова, нужен апстрим-патч UI)
2. INFRA-5 — Tailscale + Herdr reattach (один вечер; открывает INFRA-11)
3. INFRA-10 — вкладка трекера и нотификации в Hearth
4. INFRA-11 — Hearth как PWA (блокируется INFRA-5)

## Ключевые факты (rule-7: цифры живут здесь)

- Стек: herdr 0.8.2 · dsh 0.1.0-rc.7 · Node 25.8.1 · порт web `octo` — 3081.
- У `pane report-agent` 4 статуса (`idle|working|blocked|unknown`), `done` нет.
- Модель headless-прогона задаётся только `--patch`-оверлеем.
- Секреты бота — `~/.dsh-tg/env` (вне git); Telegram из python — через curl
  (trust store framework-питона пуст).
- Сквозной прогон делегирования: 1 тикет, 1 прогон, 0 ручных правок после.
